#include "main.h"

/**
 * _isupper - this is a function that checks for uppercase character
 * @c: a character argument
 * Return: returns 1, 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
