#include "main.h"
#include <string.h>

/**
 * wildcmp - this is a function that compares two strings
 * @s1: param 1
 * @s2: param 2
 * Return: 1,0
 */
int wildcmp(char *s1, char *s2)
{
	int result;

	result = wildcmp(s1, s2);
	result(result);
}
