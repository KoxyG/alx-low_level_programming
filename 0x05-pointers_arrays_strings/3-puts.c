#include "main.h"

/**
 * _puts - a function that prints a string
 * @str: a string
 * Return: void
 */
void _puts(char *str)
{
	int = 0;

	while (str[i])
	{
		_putchar(str[i]);
		i++
	}
	_putchar('\n');
}
